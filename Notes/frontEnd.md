# Ony Front-End Tasks for AI

Most of our work will be in ./src inside the styles directory
You will read and understand the files inside it, then you will edit or create a default daisyui theme inside "src/styles/vendor/daisyui.css" using the colors variables inside the src/styles/main.sass, but be aware that we have a Default accent color palette - that must be only active for default theme even it dark or light. not other daisyui theme, why only the default theme? because the default theme have a dark static colors that our accent color palette will be good with, for example I can use the --LFM-accent-green as my default accent for the default theme, but it will be ugly with the "Rose Pine" theme. Pink color with Green color really ugly. So What I expect as a result is to find an option in the settings window(view) "src/features/settings/views/SettingsView.vue" that will help the user to change the default theme it will be a dropdown menu with all our themes in "src/styles/vendor/daisyui.css", also create another option for the "accent color palette" just list of buttons(full rounded button that contain the accent color as background) beside the name of the accent color, and these buttons list will be enabled only if the user use the default theme even if its the theme dark or light, but not the other themes like "Rose Pine" even if it the "Rose Pine" theme was in the dark or the light. don't forget you can use the pinia store if it will help, also I don't to effect the performance, and respect the rules in "Project.Notes.md/1.Rules.md". also add comments with the name of the variables that we have transfer from "src/styles/main.sass" above each var for the default theme in "src/styles/vendor/daisyui.css" for example if the --color-base-100 will take the --LFM-bg value we will write it like:
// --LFM-bg
--color-base-100: #fff

This will help us to replace all the variables that we used in the front-end with the new vars in the daisyui theme. and this will be the next task.
